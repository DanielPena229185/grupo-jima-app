export interface TiendaDTO {
    id: string,
    nombre: string,
    telefono: string
}

export interface RepartidorDTO {
    id: string,
    nombres: string,
    apellidos: string
}

export interface TortilleriaDTO {
    id: string,
    nombre: string
}